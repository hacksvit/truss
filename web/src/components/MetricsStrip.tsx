import type {MetricsView} from '../contracts';
export default function MetricsStrip({metrics}:{metrics:MetricsView}){return <section className="metrics" aria-label="Measured metrics"><span>Compliance <strong>{metrics.cap_compliance_pct===null?'Not measured':`${metrics.cap_compliance_pct.toFixed(1)}%`}</strong></span><span>Time to safe <strong>{metrics.time_to_safe_ms===null?'Not measured':`${metrics.time_to_safe_ms} ms`}</strong></span><span>Lease violations <strong>{metrics.lease_model_violations===null?'No live oracle':metrics.lease_model_violations}</strong></span></section>;}
