# Used only when the app stack is `container`. Default dashboard apps are
# heroku-24 and ignore this file; they need the root Procfile instead.
build:
  docker:
    web: Dockerfile
run:
  web: uvicorn truss.hosted:create_hosted_app --factory --host 0.0.0.0 --port $PORT --workers 1
